#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str1;
	char *str2;
	char *str3;

	str1 = "Kevin is a very very bad personnnnnnnnn,\n";
	str2 = "amesema mimi na Eve tunasumbua.\n";
	str3 = "are waiting for a sincere apology - Shann.\n";
	_puts(str1);
	_puts(str2);
	_puts(str3);
	return (0);
}
